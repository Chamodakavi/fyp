import AdminSettings from "@/components/settings/AdminSettings";
import React from "react";

function page() {
  // Remove the semicolon after return and wrap in parentheses
  return <AdminSettings />;
}

export default page;
